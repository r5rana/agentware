# agentware test suite (stdlib unittest only — no third-party deps).
# Marks `tests/` as a package so `python3 -m unittest tests.test_*` works in
# addition to `python3 -m unittest discover -s tests`.
